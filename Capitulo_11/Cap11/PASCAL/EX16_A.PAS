PROGRAM EX16;
USES CRT;
TYPE vendedor = RECORD
	codigo_vendedor: INTEGER;
	nome_vendedor: STRING[20];
	mes: INTEGER;
	valor_venda: REAL;
END;
loja = FILE OF vendedor;
VAR L: loja;
    V: vendedor;
    CODIGO_VENDEDOR, MES,K: INTEGER;
    TOTAL: REAL;
 BEGIN
CLRSCR;
TOTAL:=0;
ASSIGN(L, 'C:\VENDAS.DAT');
{$I-}
RESET(L);
{$I+}
IF IORESULT = 2
   THEN REWRITE(L);
K:=FILESIZE(L);
IF K = 0
THEN WRITELN('ARQUIVO DE VENDAS VAZIO')
ELSE BEGIN
WRITE('DIGITE O C‡DIGO DO VENDEDOR A SER CONSULTADO ');
READLN(CODIGO_VENDEDOR);
WHILE NOT EOF(L) DO
BEGIN
READ(L, V);
IF (V.CODIGO_VENDEDOR = CODIGO_VENDEDOR)
THEN BEGIN
     TOTAL:= TOTAL + V.VALOR_VENDA
     END;
END;
IF TOTAL = 0
THEN WRITELN('N«O EXISTE VENDA CADASTRADA PARA ESTE VENDEDOR')
ELSE WRITELN('TOTAL DAS VENDAS R$ ',TOTAL:7:2);
END;
READLN;
CLOSE(L);
END.
